let aluno2: {
    nome: string,
    aprovado: boolean,
    idade?: number,
    notas: [number, number, number, number]
} 

aluno2 = {
    nome: 'talison',
    aprovado: true,
    notas: [ 1, 2, 3, 4 ]
}